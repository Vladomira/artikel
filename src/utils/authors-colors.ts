import { Colors } from "./colors";

export const authorsColors = [
    { border: `${Colors.LIGHT_CARROT}`, nameColor: `${Colors.CARROT}` },
    { border: `${Colors.MINT}`, nameColor: `${Colors.DEEP_MINT}` },
    { border: `${Colors.LILAC}`, nameColor: `${Colors.DEEP_LILAC}` },
];
