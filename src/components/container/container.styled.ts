import styled from "styled-components";
import { Spaces } from "../../utils/variables";

export const Container = styled.div`
    padding-left: ${Spaces.CONTAINER_PADDING};
    padding-right: ${Spaces.CONTAINER_PADDING};
    width: 100%;
    margin: 0 auto;
`;
